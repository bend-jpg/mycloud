{
  "name": "mytitancloud-mobile",
  "version": "0.1.0",
  "description": "MyTitanCloud — wrapper Capacitor pour produire .apk Android (et .ipa iOS quand compte Apple Dev dispo)",
  "private": true,
  "scripts": {
    "init:android": "cap add android",
    "init:ios": "cap add ios",
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "build:android:debug": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
